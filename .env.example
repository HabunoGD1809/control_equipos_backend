# =================================================================
#                 CONFIGURACIÓN PRINCIPAL DEL PROYECTO
# =================================================================

# Nombre del proyecto, usado en la documentación de la API.
PROJECT_NAME="Control de Equipos API"
API_V1_STR="/api/v1"

# ¡MUY IMPORTANTE! Clave secreta para la firma de tokens JWT.
# Genera una clave segura para tu entorno local/producción con: openssl rand -hex 32
SECRET_KEY=tu_clave_secreta_aqui

# Algoritmo de firma para los JWT.
ALGORITHM=HS256

# Duración del token de acceso en minutos (ej: 1440 minutos = 1 día).
ACCESS_TOKEN_EXPIRE_MINUTES=1440


# =================================================================
#                  CONFIGURACIÓN DE LA BASE DE DATOS
# =================================================================

# Host del servidor de PostgreSQL ('localhost' para desarrollo local, 'db' para Docker).
POSTGRES_SERVER=localhost
POSTGRES_PORT=5432
POSTGRES_USER=postgres
POSTGRES_PASSWORD=tu_contraseña_de_postgres
POSTGRES_DB=control_equipos_db

# Opcional: URL completa (si se prefiere sobre las partes individuales).
# DATABASE_URL=postgresql+psycopg2://postgres:password@localhost:5432/control_equipos_db


# =================================================================
#                      CONFIGURACIÓN DE CORS
# =================================================================

# Lista de orígenes permitidos separados por coma.
# Ejemplo: BACKEND_CORS_ORIGINS=http://localhost:3000,https://mi-frontend.com
BACKEND_CORS_ORIGINS=http://localhost:3000,http://localhost:8080


# =================================================================
#                 CONFIGURACIÓN DE CELERY (TAREAS ASÍNCRONAS)
# =================================================================

# URL del broker de Celery ('redis://localhost:6379/0' para desarrollo local, 'redis://redis:6379/0' para Docker).
CELERY_BROKER_URL=redis://localhost:6379/0
CELERY_RESULT_BACKEND=redis://localhost:6379/0


# =================================================================
#                    OTRAS CONFIGURACIONES (OPCIONAL)
# =================================================================

# Número de intentos de inicio de sesión fallidos antes de bloquear una cuenta.
MAX_FAILED_ATTEMPTS_BEFORE_LOCK=5

# Nivel de logs para la aplicación (DEBUG, INFO, WARNING, ERROR, CRITICAL).
# LOG_LEVEL=INFO


# =================================================================
#                    CONFIGURACIÓN DE EMAIL (OPCIONAL)
# =================================================================

# Descomenta y rellena estas líneas si necesitas enviar correos electrónicos.
# SMTP_HOST=
# SMTP_PORT=
# SMTP_USER=
# SMTP_PASSWORD=
# EMAIL_FROM=
